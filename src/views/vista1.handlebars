{{#if products.length}}
  {{#each products }}
  <div>
    <ul>
      <h1>{{this.name}}</h1>
      <h2>Price: ${{price}}</h2>
     
    </ul>
  </div>
  {{/each}}
{{else}}
  <h1>Algo a pasado, no hay productos!</h1>
{{/if}}